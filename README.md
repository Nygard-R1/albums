# React Native Albums

## Opening

```git clone <repo>```

```cd /path/to/cloned/repo```

```npm install```

```react-native start```

Open a new terminal, make sure emulator is running.

```react-native run-android```
